# InvertedWidget

The Inverted widget inverts colors of its content.

## Basic Usage

```csharp
AnsiConsole.Write(new Inverted(new Markup("Text")));
```

## Use Case

Useful for creating contrast effects:
```csharp
new Inverted(new Panel("Highlighted"));
```

See: Padder Widget
